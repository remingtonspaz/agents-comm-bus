# @agents-comm-bus/pi-core

Bundled core for agents-comm-bus Pi per-comm packages. Not user-installed standalone — install `@agents-comm-bus/pi-telegram` (or other `pi-<comm>`) instead, which bundles this core.

Source of truth: `docs/research/pi/README.md`.
