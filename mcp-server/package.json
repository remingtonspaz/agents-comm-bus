{
  "name": "telegram-mcp-server",
  "version": "1.0.0",
  "description": "Deprecated compatibility package; build host-specific MCP shims from ../hosts",
  "type": "module",
  "main": "dist/claude-mcp-shim.js",
  "scripts": {
    "start": "node dist/claude-mcp-shim.js",
    "build": "npm --prefix ../hosts run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "node-telegram-bot-api": "^0.66.0",
    "ws": "^8.18.0"
  }
}
