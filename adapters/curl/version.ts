/**
 * Curl adapter bundle version.
 *
 * Independent content-version source for the curl CommAdapter — separate from
 * `DAEMON_VERSION` and plugin package versions. Bump when shipped curl adapter
 * behavior changes.
 */
export const ADAPTER_VERSION = "0.1.0";
